# Omarchy Project Context

このファイルは、AIアシスタントが本プロジェクト（Omarchy）の文脈を即座に理解し、正確なコード修正や提案を行うための「唯一の正解（Source of Truth）」である。

## 1. プロジェクトの哲学 (Core Philosophy)
Omarchyは、macOSユーザーのためのシームレスな代替環境として設計されたArch Linuxベースのシステムである。

- **核心原則: 「Mac Parity（Macとの等価性）」**
    - 挙動、ショートカット、フォントレンダリングにおいてmacOSを模倣する。
    - Windowsや標準的なLinuxデスクトップ（GNOME/KDE等）の慣習ではなく、macOSのユーザー体験を最優先する。
- **入力戦略**
    - `keyd` を使用してカーネルレベルでキーボードレイヤーをリマップ。
    - `Command` (Super) キーを主要な修飾キー（Cmd+C/V 等）として機能させ、Tiling WMの効率性とMacの操作性を両立する。

## 2. リポジトリの関係性 (Repository Architecture)
本システムは2つの層で構成されており、ユーザー固有設定がコア設定を上書きする構造をとる。

- **コア層 (`basecamp/omarchy`)**:
    - アップストリーム。共通のインストールロジック、デフォルト設定、テーマ、メンテナンススクリプトを含む。
- **ユーザー層 (`odyu/supplement`)**:
    - 本リポジトリ。ユーザー個人のオーバーライド層。
    - **Source of Truth**: ドキュメントと `supplement` 内のコードに矛盾がある場合、`supplement` 内の実際のコードが正解とみなされる。

## 3. ディレクトリ構造 (`odyu/supplement`)
主要なファイルとディレクトリの役割は以下の通り。

- `install.sh`: ユーザー設定を適用するためのメインエントリーポイント。
- `omarchy/`: システムレベルの設定とスクリプト。
    - `keyd/`: キーリマップ設定。Macエミュレーションの心臓部。
    - `install-packages.sh`: ユーザー固有の追加パッケージ (pacman/AUR)。
- `dotfiles/`: ユーザーレベルのドットファイル。主に `stow` で管理。
    - `hyprland/`: `hyprland-overrides.conf` 等によるWM挙動のカスタマイズ。
    - `fcitx5/`: 日本語入力 (Mozc) 設定。Mac風の「かな/英数」切り替えを実現。
    - `zsh/` & `p10k/`: シェル環境とプロンプト設定。

## 4. 主要コンポーネント (Technology Stack)
| コンポーネント | 役割 | 設定のポイント |
|:---|:---|:---|
| **Hyprland** | ウィンドウマネージャ | タイリングロジック。ショートカットはベースとオーバーライドに分割管理。 |
| **keyd** | 入力リマッパー | ハードウェア層でのエミュレーション。Cmd/CtrlロジックをOSが受け取る前に処理。 |
| **fcitx5** | IME (日本語入力) | Mac風のキー操作での入力ソース切り替えを重視。 |
| **Waybar** | ステータスバー | システム情報の視覚的フィードバックと美的テーマの適用。 |

## 5. 開発ガイドライン (Guidelines)
- **冪等性 (Idempotency)**: インストールスクリプトは何度実行しても安全でなければならない。
- **設定の分離**: システムデフォルトを直接書き換えず、可能な限り `include` やオーバーライド用ファイルを使用する。
- **キーバインド原則**:
    - **アプリケーション操作**: macOS標準に準拠し、常に `Super` (Cmd) を優先。
    - **ターミナル・システム操作**: `Alt` (Option) や `Ctrl` を使用。
- **コードスタイル**:
    - シェルスクリプトは `bash/sh` を使用。パスは曖昧さを避けるため明示的に記述すること。
